# Legacy Scripts

These are the original bash scripts and config files, replaced by the Ansible
playbooks in `ansible/`. Kept for reference only.

**Do not run these scripts.** Use `ansible/setup.sh` instead.
